<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yytxdy.fim.sso.mapper.UserMapper">
    <insert id="save">
        insert ignore into t_user(telephone, create_time) values(#{telephone}, now())
    </insert>
</mapper>
